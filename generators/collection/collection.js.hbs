var Model, Collection, {{#camelize}}{{name}}{{/camelize}}, {{#camelize}}{{pluralName}}{{/camelize}};

Model = require('app/models/model');
Collection = require('app/models/collection');

{{#camelize}}{{name}}{{/camelize}} = Model.extend({

});

{{#camelize}}{{pluralName}}{{/camelize}} = Collection.extend({
  model: {{#camelize}}{{name}}{{/camelize}}

});

module.exports = {
  Model: {{#camelize}}{{name}}{{/camelize}},
  Collection: {{#camelize}}{{pluralName}}{{/camelize}}
};
